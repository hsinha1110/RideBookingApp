import { View, Text } from 'react-native';
import React, { FC } from 'react';

const Settings: FC = () => {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
};

export default Settings;
